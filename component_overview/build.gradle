apply from: rootProject.file('component.gradle')

android {
    defaultConfig {
        resourcePrefix "overview_"
    }
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])

}
